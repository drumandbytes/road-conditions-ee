# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Copied from node_modules/maplibre-gl by the postinstall script (see package.json) — worker.mjs
# imports shared.mjs by a relative path, so both must sit unhashed, side by side, as static
# public assets rather than going through Vite's hashed asset pipeline.
public/maplibre

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
